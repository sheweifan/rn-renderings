import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Item = props => {
  return (
    <View>
      <Text>item</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  
});

export default Item